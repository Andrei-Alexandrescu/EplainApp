{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "mkdir -p dist && echo 'API-only deployment. Build the extension locally.' > dist/index.html",
  "outputDirectory": "dist",
  "functions": {
    "api/explain.ts": {
      "maxDuration": 60
    }
  }
}
